#include <stdio.h>

/**
 * main - prints all possible combinations of single-digit numbers
 *
 * Return: Always 0.
 */

int main(void)
{
int num;

for (num = 0 < 10; num++)
if (num == 9)
{
putvhar('9');
}
{
putchar(num);
putchar(',');
putchar(' ');
}

return (0);
}
