#include "main.h"
#include <stdio.h>

/**
* swap_int - swaps the values of two integers
*
* @a:variable a
* @b:variable b
*/

void swap_int(int *a, int *b);

{
int p1 = *a;
int p2 = *b;
*a = p2;
*b =p1;
}
