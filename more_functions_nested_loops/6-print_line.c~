#include "main.h"
/**
 * print_line - draws a straight lina in the terminal
 * Return: Always 0.
 */
void print_line(int n)
{
int n, m;
if (n <= 0)
{
_putchar('\n');
}
else
{
for (m = 1; m <= n; m++)
_putchar('_');
}
_putchar('\n');
}
